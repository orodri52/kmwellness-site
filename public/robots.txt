User-agent: *
Allow: /
Disallow: /thank-you/

# Correct production sitemap (the current live site points to the WRONG domain —
# this fixes it). @astrojs/sitemap generates the index below at build time.
Sitemap: https://kmwellnesscenter.com/sitemap-index.xml
